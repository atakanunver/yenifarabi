# dashboard — Farabi'nin server/requirements.txt'inden BAĞIMSIZ, ayrı venv.
# Kasıtlı olarak hafif: paramiko/APScheduler yok, ssh binary + asyncio yeterli.
fastapi==0.118.0
uvicorn[standard]==0.38.0
jinja2==3.1.4
python-multipart==0.0.12
