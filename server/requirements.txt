# server/ — Faz 1 iskeleti (docs/mimari.md §3, §9, §15 [1])
# Docker yok — bu venv systemd servisi içinde (farabi-api.service) çalışacak.

fastapi==0.118.0
uvicorn[standard]==0.38.0
pydantic==2.12.4

psycopg2-binary==2.9.12
pgvector==0.5.0

# retrieval — benchmark/recall_test.py ile AYNI modeller, aynı sürümler
sentence-transformers==5.7.0
transformers==5.14.1
torch==2.13.0

# icerik.py — Faz 2, ders_icerigi/pdf_sayfa taşıması (client requirements.txt ile aynı sürüm disiplini yok, en güncel kararlı)
PyMuPDF
pdfplumber
openai
python-multipart

# dosya.py — Faz 2/8, file_processor taşıması
python-docx
python-pptx
pandas
openpyxl
Pillow
PyPDF2
